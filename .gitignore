__pycache__/
*.pyc
.pytest_cache/
pytest-cache-files-*/
.venv/
venv/
# Large cached panels are regenerated locally; keep the repo light.
data/panel_cache.json
# Local Claude Code permission grants — machine-specific, not for the repo.
.claude/settings.local.json
