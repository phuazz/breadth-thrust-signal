__pycache__/
*.pyc
.pytest_cache/
pytest-cache-files-*/
.venv/
venv/
# Large cached panels are regenerated locally; keep the repo light.
data/panel_cache.json
# Norgate vendor series. The licence is personal-use-only and this repo is
# PUBLIC — vendor values must never be committed anywhere. The cache default
# lives outside the repo entirely (see norgate_provider.default_cache_root);
# this entry only covers the case where someone points it back in here.
data/norgate/
# Local Claude Code permission grants — machine-specific, not for the repo.
.claude/settings.local.json
# Synthetic --self-test output. Fabricated numbers; never the filed record.
data/_selftest/
docs/_selftest/
# Real-data --preview rehearsal output (provider cutover dry runs).
data/_preview/
docs/_preview/
data_local/
